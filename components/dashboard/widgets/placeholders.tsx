// Retired along with the Dashboard Builder / Dashboard 2 widget-grid system.
// Left in place (rather than deleted, which this environment can't do) but
// intentionally empty — nothing imports from this file anymore. Safe to
// delete by hand if you want it gone from the repo.
export {};
